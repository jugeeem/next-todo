import TodosIndex from '@/features/todos/components/Index';

export const metadata = {
  title: 'Todo一覧',
  description: 'Todo一覧ページ',
};

export default function TodosPage() {
  return <TodosIndex />;
}
